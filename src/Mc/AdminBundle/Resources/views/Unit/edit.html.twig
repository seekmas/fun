{% extends '::bootstrap3.html.twig' %}

{% block left %}

    <ul>
        {% for key , u in entities %}
        <li><a href="{{ url('admin_unit_edit' , { id : u.id })}}">{{key+1}} . {{u.subject}}</a></li>
        {% endfor %}
    </ul>

{% endblock %}



{% block body -%}
    <h1>Unit edit</h1>

    {{ form_start(edit_form) }}

        <div class="control-group">
            {{form_row( edit_form)}}
        </div>
        
    {{ form_end(edit_form) }}


    <a href="{{ path('admin_unit') }}">
        返回页面列表
    </a>


    {{ form(delete_form) }}

{% endblock %}
